import styled from '@emotion/styled';

const Quote = styled.blockquote`
  position: relative;
  font-size: 16px;
   letter-spacing: 1px;
  line-height: 1.2;
  
  p {
    margin-top: 16px;
    color: rgb(255, 107, 8);;
  }

  svg {
    margin-right: 16px;
  }

  cite {
    font-style: normal;
  }
`;

export { Quote };
